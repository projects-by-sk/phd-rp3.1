When running the code on CPU
%pip install transformers datasets torch scikit-learn -q
%pip install torch==2.3.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu -q
%pip install 'accelerate>=0.26.0
%pip install -U "transformers[torch]" "accelerate>=0.26.0" -q
%pip install -U jupyter ipywidgets tqdm -q
%pip install -U transformers -q

device = torch.device("cpu")
model.to(device)

When running the code on GPU
!pip install transformers datasets torch scikit-learn -q
!pip install torch==2.3.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu -q
!pip install 'accelerate>=0.26.0
!pip install -U "transformers[torch]" "accelerate>=0.26.0" -q
!pip install -U jupyter ipywidgets tqdm -q
!pip install -U transformers -q
